<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title><%= htmlWebpackPlugin.options.title %></title>
  </head>
  <body
    style="
      background-color: black;
      margin: 0;
      padding: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100vw;
      height: 100vh;
      cursor: none;
  ">
    <div id="play-button" style="
      position: fixed;
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: black;
      z-index: 999;
      cursor: default;
      width: 100vw;
      height: 100vh;
    ">
      <img src="./play.svg"/>
    </div>
    <div id="display">
      <canvas width=<%= screenSize.width %> height=<%= screenSize.height %> id="screen"></canvas>
    </div>
    <style>
      @keyframes fade {
        from {opacity: 1;}
        to {opacity: 0;}
      }
      .fadeOut{
        animation-duration: 1.0s;
        animation-name: fade;
      }
      .input{
        position: absolute;
        background-color: transparent;
        border: none;
        cursor: none;
        z-index: 80;
      }
      .input:focus{
        outline: none;
      }
      #display{
        position: absolute;
        width: 100%;
        height: 100%;
        max-width: calc(100vh * 4 / 3);
        max-height: calc(100vw * 3 / 4);
      }
      #screen{
        aspect-ratio: <%= screenSize.width %> / <%= screenSize.height %>;
        object-fit: contain;
        object-position: center;
        cursor: none;
        width: 100%;
        height: 100%;
      }
      #play-button img{
        height: 50vh;
        width: 50vw;
      }
      #play-button img:hover{
        filter: invert(1);
      }
    </style>
  </body>
</html>